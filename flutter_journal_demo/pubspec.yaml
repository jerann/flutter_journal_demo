name: flutter_journal_demo
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^0.12.10
  firebase_auth: ^0.14.0+8
  firebase: ^6.0.0 #Must be ENABLED for WEB, and DISABLED for MOBILE

  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
